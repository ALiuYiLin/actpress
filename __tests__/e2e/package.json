{
  "name": "tests-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "watch": "DEBUG=1 vitest",
    "site:dev": "actpress",
    "site:build": "vitepress build",
    "site:preview": "vitepress preview"
  },
  "devDependencies": {
    "actpress": "workspace:*"
  }
}
